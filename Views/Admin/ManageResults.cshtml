@model List<TorneioTenisMesa.Models.ViewModels.GameResultViewModel>

@{
    ViewData["Title"] = "Gerenciar Resultados";
}

<h2>@ViewData["Title"]</h2>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">
        @TempData["SuccessMessage"]
    </div>
}

@if (Model.Count == 0)
{
    <div class="alert alert-info">Nenhum jogo disponível para inserir resultados.</div>
}
else
{
    <form asp-action="UpdateGameResults" method="post">
        @Html.AntiForgeryToken()

        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>Jogador 1</th>
                    <th>Placar</th>
                    <th>Jogador 2</th>
                    <th>Placar</th>
                    <th>Grupo</th>
                    <th>Data</th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.Count; i++)
                {
                    <tr>
                        <td>@Model[i].Player1Name</td>
                        <td>
                            <input type="number" asp-for="@Model[i].ScorePlayer1" class="form-control" />
                            <input type="hidden" asp-for="@Model[i].GameId" />
                        </td>
                        <td>@Model[i].Player2Name</td>
                        <td>
                            <input type="number" asp-for="@Model[i].ScorePlayer2" class="form-control" />
                        </td>
                        <td>@Model[i].Group</td>
                        <td>@Model[i].Date.ToString("dd/MM/yyyy")</td>
                    </tr>
                }
            </tbody>
        </table>

        <button type="submit" class="btn btn-success">Salvar Resultados</button>
    </form>
}

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
